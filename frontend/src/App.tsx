import { MantineProvider } from '@mantine/core';
import { Route, Routes } from 'react-router-dom';

export function App() {
  return (
    <MantineProvider>
      <Routes>
        {/* Маршруты §7.1 добавятся на шаге 3 */}
      </Routes>
    </MantineProvider>
  );
}
